class UserModel {
  String id, email, password, name, street, county, about, phone;

  UserModel() {
    this.id = this.email = this.password =
        this.name = this.street = this.county = this.about = this.phone = "";
  }
}
